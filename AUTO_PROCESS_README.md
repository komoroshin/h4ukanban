# 🤖 Автоматическая обработка встреч Healthy4U

Система автоматически:
- ✅ Проверяет новые встречи в Fireflies каждый час
- ✅ Фильтрует встречи по healthy4u (по ключевым словам в названии)
- ✅ Анализирует транскрипты через Claude AI
- ✅ Извлекает action items, решения, ключевые темы
- ✅ Добавляет задачи в канбан автоматически
- ✅ Задачи появляются через ~1 час после встречи

---

## 🚀 Быстрый старт

### 1. Запустите настройку

```bash
chmod +x setup_auto_process.sh
./setup_auto_process.sh
```

Скрипт автоматически:
- Проверит окружение и зависимости
- Создаст необходимые папки
- Настроит автоматический запуск (launchd на macOS или cron на Linux)
- Выполнит тестовый запуск

### 2. Проверьте результат

После первого запуска появятся:
- `meetings/.auto_process_state.json` - состояние обработки
- `logs/auto_process.log` - логи
- Обновленный `meetings/action_items/kanban.html`

### 3. Готово!

Теперь система работает автоматически. Через час после каждой встречи задачи появятся в канбане.

---

## 🔧 Ручной запуск

Если не хотите ждать час, запустите обработку вручную:

```bash
chmod +x process_now.sh
./process_now.sh
```

Или напрямую:

```bash
source venv/bin/activate
source .env
python3 auto_process_meetings.py
```

---

## 📊 Управление

### macOS (launchd)

```bash
# Статус
launchctl list | grep healthy4u

# Остановить
launchctl unload ~/Library/LaunchAgents/com.healthy4u.autoprocess.plist

# Запустить
launchctl load ~/Library/LaunchAgents/com.healthy4u.autoprocess.plist

# Запустить сейчас вручную
launchctl start com.healthy4u.autoprocess
```

### Linux (cron)

```bash
# Посмотреть задания
crontab -l

# Редактировать
crontab -e

# Логи cron
grep CRON /var/log/syslog
```

### Просмотр логов

```bash
# В реальном времени
tail -f logs/auto_process.log

# Последние 50 строк
tail -50 logs/auto_process.log

# Поиск ошибок
grep "ОШИБКА" logs/auto_process.log
```

---

## 📁 Структура файлов

```
healthy4u/
├── auto_process_meetings.py       # Основной скрипт обработки
├── setup_auto_process.sh          # Настройка автозапуска
├── process_now.sh                 # Ручной запуск
├── run_auto_process.sh            # Обертка для cron/launchd (создается автоматически)
│
├── meetings/
│   ├── .auto_process_state.json   # Состояние: какие встречи обработаны
│   └── action_items/
│       ├── kanban.html            # Канбан-доска (обновляется автоматически)
│       ├── tasks_clean.json       # База задач
│       ├── analysis_*.json        # Результаты анализа каждой встречи
│       └── shturm_analysis.json
│
└── logs/
    ├── auto_process.log           # Логи автообработки
    ├── launchd.out.log           # Stdout launchd (macOS)
    └── launchd.err.log           # Stderr launchd (macOS)
```

---

## ⚙️ Как это работает

### 1. Мониторинг встреч

Каждый час скрипт:
1. Подключается к Fireflies API
2. Получает последние 50 встреч
3. Фильтрует по ключевым словам: `healthy4u`, `h4u`, `дейлик`, `штурм`
4. Выбирает встречи за последние 2 часа
5. Проверяет, не обработаны ли уже (по ID в state файле)

### 2. Анализ через Claude

Для каждой новой встречи:
1. Получает полный транскрипт из Fireflies
2. Форматирует по спикерам
3. Отправляет Claude с промптом для извлечения:
   - Action items (задачи, исполнители, дедлайны, приоритеты)
   - Решения
   - Ключевые темы
4. Парсит JSON ответ от Claude

### 3. Обновление канбана

После анализа:
1. Добавляет задачи в `tasks_clean.json`
2. Обновляет статистику (счетчики по приоритетам/исполнителям)
3. Регенерирует `kanban.html` с новыми задачами
4. Сохраняет ID встречи как обработанной

### 4. Логирование

Все действия логируются с timestamp:
- Найденные встречи
- Начало/конец обработки
- Добавленные задачи
- Ошибки

---

## 🔍 Фильтрация встреч

По умолчанию обрабатываются встречи с ключевыми словами в названии:

- `healthy4u`
- `h4u`
- `дейлик`
- `штурм`

### Изменить фильтры

Отредактируйте [auto_process_meetings.py](auto_process_meetings.py):

```python
# Строка ~175
if any(keyword in title for keyword in ['healthy4u', 'h4u', 'дейлик', 'штурм']):
    h4u_meetings.append(t)
```

Или используйте фильтр по участникам (как в `config.json`).

---

## 🐛 Решение проблем

### Задачи не появляются

1. Проверьте логи:
   ```bash
   tail -f logs/auto_process.log
   ```

2. Проверьте, что встречи есть в Fireflies:
   ```bash
   python3 get_shturm.py
   ```

3. Проверьте название встречи - должно содержать ключевые слова

4. Запустите обработку вручную:
   ```bash
   ./process_now.sh
   ```

### Ошибка API

```bash
# Проверьте переменные окружения
echo $FIREFLIES_API_KEY
echo $ANTHROPIC_API_KEY

# Проверьте .env
cat .env
```

### Автозапуск не работает

**macOS:**
```bash
# Проверьте статус
launchctl list | grep healthy4u

# Посмотрите логи
cat logs/launchd.err.log
```

**Linux:**
```bash
# Проверьте crontab
crontab -l

# Проверьте права
ls -la run_auto_process.sh
chmod +x run_auto_process.sh
```

### Канбан не обновляется

1. Проверьте файл задач:
   ```bash
   cat meetings/action_items/tasks_clean.json
   ```

2. Обновите страницу в браузере (Cmd+R)

3. Очистите кэш браузера (Cmd+Shift+R)

---

## 🔄 Сброс и очистка

### Начать с нуля

```bash
# Удалить состояние (все встречи будут обработаны заново)
rm meetings/.auto_process_state.json

# Очистить задачи
rm meetings/action_items/tasks_clean.json

# Очистить логи
rm logs/auto_process.log

# Запустить обработку
./process_now.sh
```

### Удалить автозапуск

**macOS:**
```bash
launchctl unload ~/Library/LaunchAgents/com.healthy4u.autoprocess.plist
rm ~/Library/LaunchAgents/com.healthy4u.autoprocess.plist
```

**Linux:**
```bash
crontab -e
# Удалите строку с healthy4u
```

---

## 🎯 Настройка интервала

### Изменить частоту проверки

По умолчанию: каждый час

**macOS (launchd):**

Отредактируйте `~/Library/LaunchAgents/com.healthy4u.autoprocess.plist`:

```xml
<key>StartInterval</key>
<integer>1800</integer>  <!-- 30 минут = 1800 секунд -->
```

Перезагрузите:
```bash
launchctl unload ~/Library/LaunchAgents/com.healthy4u.autoprocess.plist
launchctl load ~/Library/LaunchAgents/com.healthy4u.autoprocess.plist
```

**Linux (cron):**

```bash
crontab -e
```

Измените строку:
```cron
# Каждые 30 минут
*/30 * * * * /path/to/run_auto_process.sh

# Каждые 15 минут
*/15 * * * * /path/to/run_auto_process.sh

# Каждый час (по умолчанию)
0 * * * * /path/to/run_auto_process.sh
```

### Изменить окно обработки

По умолчанию обрабатываются встречи за последние 2 часа.

Отредактируйте [auto_process_meetings.py](auto_process_meetings.py):

```python
# Строка ~201
if is_recent_meeting(meeting_date, hours=2):  # Измените на 1, 3, 4, и т.д.
```

---

## 📈 Статистика

После каждого запуска в логах:

```
[2026-01-28 14:00:00] Запуск автоматической обработки встреч healthy4u
[2026-01-28 14:00:01] Получение списка встреч из Fireflies...
[2026-01-28 14:00:02] Найдено встреч: 47
[2026-01-28 14:00:02] Встреч healthy4u: 8
[2026-01-28 14:00:02] Новых встреч для обработки: 1
[2026-01-28 14:00:02] Обработка: Meet – Дейлик
[2026-01-28 14:00:15] Добавлено задач: 5 из встречи 'Meet – Дейлик'
[2026-01-28 14:00:15] Всего добавлено задач: 5
[2026-01-28 14:00:16] Канбан обновлен: 21 задач
[2026-01-28 14:00:16] Обработка завершена успешно
```

---

## 🔗 Интеграция с Notion (будущее)

После настройки базовой системы можно добавить:

1. Notion API для синхронизации задач
2. Двустороннюю синхронизацию (канбан ↔️ Notion)
3. Уведомления в Telegram/Slack
4. Webhook для мгновенной обработки после встречи

---

## 💡 Советы

1. **Первый запуск**: Используйте `./process_now.sh` для немедленной обработки

2. **Мониторинг**: Добавьте `tail -f logs/auto_process.log` в алиас:
   ```bash
   echo "alias h4u-logs='tail -f ~/path/to/healthy4u/logs/auto_process.log'" >> ~/.bashrc
   ```

3. **Названия встреч**: Убедитесь, что встречи содержат ключевые слова (`h4u`, `healthy4u`)

4. **Дедлайны**: Claude извлекает дедлайны из текста встречи ("завтра", "через неделю")

5. **Приоритеты**: Claude определяет приоритеты автоматически на основе контекста

---

**Готово!** Теперь ваши задачи будут появляться автоматически 🎉
