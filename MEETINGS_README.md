# Автоматизация обработки транскриптов встреч healthy4u

Система для автоматической обработки транскриптов встреч из Fireflies с извлечением action items и задач с помощью Claude AI.

## 🎯 Что делает система

1. **Синхронизация** - автоматически загружает транскрипты встреч из Fireflies
2. **Фильтрация** - выбирает только встречи по проекту healthy4u (по участникам)
3. **Анализ** - извлекает action items, решения, ключевые темы с помощью Claude AI
4. **Организация** - сохраняет задачи в структурированном виде
5. **Отчеты** - генерирует markdown отчеты с приоритетами задач

## 📋 Требования

- Python 3.8+
- API ключ Fireflies
- API ключ Anthropic (Claude)

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
# Активируйте виртуальное окружение (если есть)
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

### 2. Настройка API ключей

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Отредактируйте `.env` и добавьте ваши API ключи:

```env
FIREFLIES_API_KEY=ваш_ключ_fireflies
ANTHROPIC_API_KEY=ваш_ключ_anthropic
```

**Где получить API ключи:**

- **Fireflies**: https://app.fireflies.ai/integrations/custom/fireflies
  1. Войдите в Fireflies
  2. Settings → Integrations → API
  3. Создайте новый API ключ

- **Anthropic**: https://console.anthropic.com/
  1. Зарегистрируйтесь / войдите
  2. API Keys → Create Key

### 3. Настройка фильтрации встреч

Отредактируйте файл `config.json` и укажите email адреса участников команды healthy4u:

```json
{
  "project_name": "healthy4u",
  "team_members": [
    "ваш_email@example.com",
    "коллега@example.com"
  ],
  "keywords": [
    "healthy4u",
    "H4U",
    "здоровье"
  ]
}
```

Или используйте интерактивную настройку:

```bash
python process_meetings.py --config
```

### 4. Запуск обработки

```bash
# Обработать последние 20 встреч
python process_meetings.py

# Обработать последние 50 встреч
python process_meetings.py --limit 50

# Показать текущие открытые задачи
python process_meetings.py --show-tasks
```

## 📁 Структура проекта

```
healthy4u/
├── meetings/
│   ├── transcripts/          # Сырые транскрипты из Fireflies
│   ├── processed/            # Обработанные данные с action items
│   └── action_items/
│       ├── all_tasks.json    # База всех задач
│       └── tasks_report.md   # Markdown отчет по задачам
├── fireflies_client.py       # Клиент для Fireflies API
├── transcript_processor.py   # Обработчик транскриптов с Claude AI
├── process_meetings.py       # Главный скрипт
├── config.json               # Конфигурация проекта
├── requirements.txt          # Python зависимости
└── .env                      # API ключи (не коммитить!)
```

## 🔧 Использование

### Основные команды

```bash
# Синхронизация и обработка транскриптов
python process_meetings.py

# Обработать больше встреч
python process_meetings.py --limit 50

# Переобработать все транскрипты заново
python process_meetings.py --force-reprocess

# Показать список открытых задач
python process_meetings.py --show-tasks

# Настроить конфигурацию
python process_meetings.py --config
```

### Использование отдельных модулей

#### Fireflies Client

```python
from fireflies_client import FirefliesClient

# Создание клиента
client = FirefliesClient()

# Синхронизация последних 10 встреч только по проекту
client.sync_transcripts(limit=10, filter_by_project=True)

# Получить конкретный транскрипт
transcript = client.get_transcript_by_id("transcript_id")
```

#### Transcript Processor

```python
from transcript_processor import TranscriptProcessor

# Создание процессора
processor = TranscriptProcessor()

# Обработка файла транскрипта
processor.process_transcript_file("meetings/transcripts/2026-01-27_xxxxx.json")

# Получить открытые задачи
tasks = processor.get_open_tasks()

# Сгенерировать отчет
report = processor.generate_tasks_report()
print(report)
```

## 📊 Формат данных

### Извлеченные action items

Каждый action item содержит:

- **task** - описание задачи
- **assignee** - ответственный (если упоминался)
- **deadline** - срок (если упоминался)
- **priority** - приоритет (high/medium/low)
- **context** - контекст из обсуждения
- **status** - статус (open/completed)
- **meeting_title** - название встречи
- **meeting_date** - дата встречи

### Структура обработанных данных

```json
{
  "meeting_id": "xxx",
  "meeting_title": "Встреча по healthy4u",
  "meeting_date": "2026-01-27",
  "processed_at": "2026-01-27T12:00:00",
  "extracted_data": {
    "action_items": [...],
    "decisions": [...],
    "key_topics": [...],
    "next_steps": [...],
    "summary": "..."
  }
}
```

## 🔄 Автоматизация

Для регулярной автоматической обработки можно настроить cron:

```bash
# Редактировать crontab
crontab -e

# Добавить задачу (каждый день в 9:00)
0 9 * * * cd /path/to/healthy4u && /path/to/venv/bin/python process_meetings.py >> logs/meetings.log 2>&1
```

Или использовать GitHub Actions / другой CI/CD.

## 🐛 Решение проблем

### Нет транскриптов после синхронизации

1. Проверьте правильность API ключа Fireflies
2. Убедитесь, что в `config.json` указаны правильные email участников
3. Проверьте, есть ли встречи в Fireflies за указанный период
4. Попробуйте увеличить `--limit`

### Ошибка при обработке транскриптов

1. Проверьте API ключ Anthropic
2. Убедитесь, что на аккаунте Anthropic есть кредиты
3. Проверьте логи для деталей ошибки

### Не фильтруются встречи по проекту

1. Проверьте, что email в `config.json` совпадают с email участников в Fireflies
2. Попробуйте добавить ключевые слова в `keywords` в config.json

## 💡 Советы

1. **Регулярная синхронизация** - запускайте скрипт регулярно (раз в день или неделю)
2. **Проверка задач** - используйте `--show-tasks` для быстрого просмотра открытых задач
3. **Отчеты** - файл `meetings/action_items/tasks_report.md` удобно смотреть в GitHub/VS Code
4. **Фильтрация** - настройте `team_members` максимально точно для лучшей фильтрации
5. **Лимит** - начните с небольшого лимита (10-20) для тестирования

## 📝 Поддержка

При возникновении проблем:

1. Проверьте логи выполнения
2. Убедитесь, что все API ключи валидны
3. Проверьте формат файлов конфигурации
4. Попробуйте запустить с флагом `--force-reprocess`

---

**Создано для проекта healthy4u**
