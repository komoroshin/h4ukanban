# Настройка обработки транскриптов встреч healthy4u через MCP

Fireflies предоставляет официальный MCP (Model Context Protocol) сервер для интеграции с Claude. Это более современный и эффективный способ работы с транскриптами встреч.

## 🎯 Преимущества MCP подхода

- ✅ Прямая интеграция с Claude без дополнительного кода
- ✅ Автоматическая работа с API Fireflies
- ✅ Встроенная безопасность и контроль доступа
- ✅ Не нужно писать клиентский код для Fireflies API
- ✅ Claude напрямую работает с вашими встречами

## 📋 Требования

- API ключ Fireflies
- Claude Desktop App или Claude Code (CLI)
- Node.js (для запуска MCP сервера)

## 🚀 Быстрая настройка

### Шаг 1: Получите API ключ Fireflies

1. Войдите на [Fireflies.ai](https://app.fireflies.ai)
2. Перейдите в **Settings → Integrations → API**
3. Создайте новый API ключ
4. Скопируйте ключ

### Шаг 2: Настройте переменные окружения

Создайте файл `.env`:

```bash
FIREFLIES_API_KEY=ваш_ключ_fireflies
ANTHROPIC_API_KEY=ваш_ключ_anthropic
```

### Шаг 3: Настройте MCP сервер

#### Для Claude Desktop App

Отредактируйте конфигурацию Claude Desktop:

**macOS**: `~/Library/Application Support/Claude/claude_desktop_config.json`

**Windows**: `%APPDATA%\Claude\claude_desktop_config.json`

Добавьте конфигурацию Fireflies MCP:

```json
{
  "mcpServers": {
    "fireflies": {
      "command": "npx",
      "args": ["-y", "@firefliesai/mcp-server-fireflies"],
      "env": {
        "FIREFLIES_API_KEY": "ваш_ключ_fireflies"
      }
    }
  }
}
```

#### Для Claude Code (CLI)

Настройка уже есть в файле `mcp_config.json` в корне проекта.

Экспортируйте переменную окружения:

```bash
export FIREFLIES_API_KEY='ваш_ключ_fireflies'
```

### Шаг 4: Перезапустите Claude

После настройки перезапустите Claude Desktop App или Claude Code CLI.

## 💡 Использование

### Базовые команды через Claude

После настройки MCP вы можете просто говорить Claude:

```
"Покажи мне последние 5 встреч по проекту healthy4u"

"Извлеки все action items из встречи от 25 января"

"Найди все встречи, где обсуждались метрики и KPI"

"Создай отчет по задачам из последних 10 встреч про healthy4u"
```

Claude автоматически подключится к Fireflies через MCP и получит нужные данные.

### Пример workflow

1. **Запрос транскриптов**:
   ```
   Найди все встречи по healthy4u за последнюю неделю
   ```

2. **Анализ встречи**:
   ```
   Проанализируй встречу от [дата] и извлеки:
   - Action items с ответственными
   - Принятые решения
   - Ключевые метрики и цифры
   - Следующие шаги
   ```

3. **Создание отчета**:
   ```
   Создай markdown отчет со всеми открытыми задачами
   по проекту healthy4u, сгруппированными по приоритетам
   ```

## 🔧 Расширенная настройка

### Создание автоматического процессора

Для регулярной автоматической обработки создайте скрипт:

```python
# process_meetings_mcp.py
#!/usr/bin/env python3
"""
Автоматическая обработка транскриптов через MCP
"""

import anthropic
import os
import json
from datetime import datetime, timedelta

def process_recent_meetings():
    """Обрабатывает последние встречи и извлекает action items"""

    client = anthropic.Anthropic(api_key=os.getenv("ANTHROPIC_API_KEY"))

    prompt = f"""
    Используя MCP сервер Fireflies, выполни следующие действия:

    1. Получи список последних 20 встреч
    2. Отфильтруй только встречи по проекту healthy4u (участники: {get_team_members()})
    3. Для каждой встречи извлеки:
       - Action items с приоритетами
       - Ответственных за задачи
       - Дедлайны
       - Принятые решения
    4. Создай JSON файл с результатами
    5. Создай markdown отчет с задачами по приоритетам

    Сохрани результаты в папку meetings/action_items/
    """

    response = client.messages.create(
        model="claude-sonnet-4-5-20250929",
        max_tokens=8000,
        messages=[{"role": "user", "content": prompt}]
    )

    return response

def get_team_members():
    """Загружает список участников команды из конфигурации"""
    with open('config.json', 'r') as f:
        config = json.load(f)
    return ', '.join(config.get('team_members', []))

if __name__ == "__main__":
    print("🔄 Запуск автоматической обработки встреч...")
    result = process_recent_meetings()
    print("✅ Обработка завершена!")
    print(result.content[0].text)
```

### Интеграция с cron

```bash
# Добавьте в crontab для автоматического запуска
0 9 * * * cd /path/to/healthy4u && python process_meetings_mcp.py
```

## 📊 Доступные возможности MCP

Через Fireflies MCP сервер доступно:

- ✅ Получение списка транскриптов
- ✅ Чтение полных транскриптов
- ✅ Метаданные встреч (дата, участники, длительность)
- ✅ Информация о спикерах
- ✅ Саммари встреч
- ✅ Поиск по встречам

## 🔒 Безопасность

- Claude и Anthropic **НЕ используют** транскрипты для обучения моделей
- Соблюдаются права доступа Fireflies - только те встречи, к которым у вас есть доступ
- API ключ хранится локально в конфигурации

## 🐛 Решение проблем

### MCP сервер не подключается

1. Проверьте, что Node.js установлен: `node --version`
2. Убедитесь, что API ключ правильный
3. Перезапустите Claude
4. Проверьте логи Claude

### Не находятся встречи

1. Убедитесь, что у вас есть доступ к встречам в Fireflies
2. Проверьте, что встречи были записаны недавно
3. Уточните фильтр поиска (дата, участники, keywords)

### Ошибка при установке MCP сервера

```bash
# Попробуйте установить глобально
npm install -g @firefliesai/mcp-server-fireflies

# Проверьте установку
npx @firefliesai/mcp-server-fireflies --version
```

## 📚 Дополнительные ресурсы

- [Официальная документация Fireflies MCP](https://guide.fireflies.ai/articles/8272956938-learn-about-the-fireflies-mcp-server-model-context-protocol)
- [Model Context Protocol спецификация](https://modelcontextprotocol.io/specification/2025-11-25)
- [Блог Fireflies про MCP](https://fireflies.ai/blog/fireflies-mcp-server/)

## 🎯 Следующие шаги

1. Настройте MCP сервер следуя инструкциям выше
2. Попробуйте базовые запросы к встречам через Claude
3. Создайте автоматический процессор для регулярной обработки
4. Настройте cron для автоматизации

---

**Источники:**
- [Fireflies MCP Connectors for ChatGPT & Claude](https://guide.fireflies.ai/articles/8272956938-learn-about-the-fireflies-mcp-server-model-context-protocol)
- [Fireflies MCP Server: Connect Your AI Tools Directly to Your Meeting Data](https://fireflies.ai/blog/fireflies-mcp-server/)
- [Model Context Protocol - Wikipedia](https://en.wikipedia.org/wiki/Model_Context_Protocol)
- [Specification - Model Context Protocol](https://modelcontextprotocol.io/specification/2025-11-25)
- [Fireflies Becomes First AI Meeting Tool in Claude's MCP Directory](https://www.businesswire.com/news/home/20250821166974/en/Fireflies-Becomes-First-AI-Meeting-Tool-in-Claudes-MCP-Directory)
