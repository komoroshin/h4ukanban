# Быстрый старт - Автоматическая обработка встреч healthy4u

## 🎯 Что это делает

Система автоматически:
1. ✅ Проверяет новые встречи в Fireflies каждые 30 минут
2. ✅ Фильтрует только встречи по проекту healthy4u (по участникам из команды)
3. ✅ Извлекает action items, решения, ключевые темы через Claude AI
4. ✅ Сохраняет задачи с приоритетами
5. ✅ Генерирует markdown отчеты
6. ✅ Работает непрерывно в фоне

## 📋 Установка (один раз)

### 1. Установите зависимости

```bash
pip install -r requirements.txt
```

### 2. Настройте API ключи

Создайте файл `.env`:

```bash
cp .env.example .env
nano .env  # или используйте любой редактор
```

Добавьте ваши ключи:

```env
FIREFLIES_API_KEY=ваш_ключ_от_fireflies
ANTHROPIC_API_KEY=ваш_ключ_от_anthropic
```

**Где получить ключи:**
- Fireflies: https://app.fireflies.ai/integrations/custom/fireflies
- Anthropic: https://console.anthropic.com/

Загрузите переменные:

```bash
source .env  # или export вручную
```

### 3. Настройте команду проекта

Отредактируйте [config.json](config.json) и укажите email участников команды healthy4u:

```json
{
  "project_name": "healthy4u",
  "team_members": [
    "ваш_email@example.com",
    "коллега@example.com"
  ]
}
```

**Важно:** Только встречи с этими участниками будут обрабатываться.

### 4. Настройте Fireflies MCP сервер

Для работы через MCP нужно настроить сервер.

**Если используете Claude Code (CLI):**

Скопируйте конфигурацию:

```bash
cp mcp_config.json ~/.config/claude/mcp_servers.json
```

**Если используете Claude Desktop:**

Добавьте в конфигурацию Claude Desktop (`~/Library/Application Support/Claude/claude_desktop_config.json`):

```json
{
  "mcpServers": {
    "fireflies": {
      "command": "npx",
      "args": ["-y", "@firefliesai/mcp-server-fireflies"],
      "env": {
        "FIREFLIES_API_KEY": "ваш_ключ_fireflies"
      }
    }
  }
}
```

Перезапустите Claude Desktop.

## 🚀 Использование

### Запуск автоматического мониторинга

```bash
# Запустить мониторинг в фоне (проверка каждые 30 минут)
./start_monitor.sh

# Или с другим интервалом (например, каждые 15 минут)
./start_monitor.sh 15
```

После запуска мониторинг будет работать непрерывно в фоне.

### Управление мониторингом

```bash
# Проверить статус
./status_monitor.sh

# Посмотреть логи в реальном времени
tail -f logs/monitor.log

# Остановить мониторинг
./stop_monitor.sh
```

### Одноразовая проверка

Если не нужен непрерывный мониторинг:

```bash
# Проверить встречи один раз
python monitor_meetings.py --once
```

## 📊 Результаты

Все результаты сохраняются в папке [meetings/action_items/](meetings/action_items/):

- **tasks_report.md** - актуальный отчет по всем открытым задачам
- **all_tasks.json** - полная база всех задач (JSON)
- **batch_YYYYMMDD_HHMMSS.json** - результаты каждой обработки

### Просмотр задач

```bash
# Посмотреть текущие задачи
cat meetings/action_items/tasks_report.md

# Или откройте в любом markdown редакторе
```

## 🔄 Автозапуск при перезагрузке (опционально)

### Через crontab

```bash
crontab -e
```

Добавьте:

```cron
# Запуск мониторинга при перезагрузке
@reboot cd /путь/к/healthy4u && /путь/к/healthy4u/start_monitor.sh

# Или запускать каждый день в 9:00 (если нужна одноразовая проверка)
0 9 * * * cd /путь/к/healthy4u && python3 monitor_meetings.py --once
```

### Через systemd (Linux)

Создайте файл `/etc/systemd/system/healthy4u-monitor.service`:

```ini
[Unit]
Description=healthy4u Meetings Monitor
After=network.target

[Service]
Type=simple
User=ваш_юзер
WorkingDirectory=/путь/к/healthy4u
Environment="ANTHROPIC_API_KEY=ваш_ключ"
Environment="FIREFLIES_API_KEY=ваш_ключ"
ExecStart=/usr/bin/python3 monitor_meetings.py --interval 30
Restart=always

[Install]
WantedBy=multi-user.target
```

Активируйте:

```bash
sudo systemctl enable healthy4u-monitor
sudo systemctl start healthy4u-monitor
sudo systemctl status healthy4u-monitor
```

## 🎨 Интерактивная работа (через Claude)

Вместо автоматики можно работать интерактивно через Claude:

```bash
# Запустите Claude Code
claude

# Или используйте Claude Desktop App
```

Примеры запросов:

```
"Покажи последние 5 встреч по healthy4u"

"Извлеки action items из встречи от 25 января и создай таблицу с приоритетами"

"Найди все встречи где обсуждались метрики CAC и LTV"

"Создай отчет по всем открытым задачам, сгруппированным по ответственным"
```

## 🐛 Решение проблем

### Мониторинг не запускается

```bash
# Проверьте переменные окружения
echo $ANTHROPIC_API_KEY
echo $FIREFLIES_API_KEY

# Проверьте конфигурацию
cat config.json

# Проверьте логи
cat logs/monitor.log
```

### Не находятся встречи

1. Убедитесь, что email в `config.json` правильные
2. Проверьте, что встречи есть в Fireflies
3. Попробуйте запустить одноразовую проверку с выводом:

```bash
python monitor_meetings.py --once
```

### MCP не работает

```bash
# Проверьте установку MCP сервера
npx @firefliesai/mcp-server-fireflies --version

# Перезапустите Claude Desktop
```

## 📈 Пример workflow

1. **Утром** - проверьте статус:
   ```bash
   ./status_monitor.sh
   ```

2. **Посмотрите новые задачи:**
   ```bash
   cat meetings/action_items/tasks_report.md
   ```

3. **При необходимости** - интерактивный анализ через Claude:
   ```
   "Покажи задачи высокого приоритета с дедлайнами на этой неделе"
   ```

4. **Вечером** - мониторинг автоматически обработал все новые встречи

## 💡 Полезные команды

```bash
# Посмотреть сколько встреч обработано
cat meetings/.monitor_state.json | grep total

# Найти все задачи по ключевому слову
cat meetings/action_items/all_tasks.json | grep -i "метрика"

# Очистить состояние (начать с нуля)
rm meetings/.monitor_state.json

# Посмотреть последние обработанные встречи
ls -lt meetings/action_items/batch_*.json | head -5
```

## 📚 Дополнительная документация

- [MEETINGS_SETUP_MCP.md](MEETINGS_SETUP_MCP.md) - подробная настройка MCP
- [MEETINGS_README.md](MEETINGS_README.md) - работа через прямой API
- [config.json](config.json) - конфигурация проекта

## 🎯 Быстрая шпаргалка

```bash
# Старт
./start_monitor.sh

# Статус
./status_monitor.sh

# Логи
tail -f logs/monitor.log

# Стоп
./stop_monitor.sh

# Задачи
cat meetings/action_items/tasks_report.md
```

---

Всё готово! Теперь ваша система будет автоматически обрабатывать встречи healthy4u.
